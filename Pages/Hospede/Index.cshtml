@page
@model sistema_quinta_do_ypua.Pages_Hospede.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].Sobrenome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].Cpf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hospede[0].QuantidadePessoas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Hospede) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sobrenome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadePessoas)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
